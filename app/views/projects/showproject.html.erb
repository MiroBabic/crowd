    <% if @project.enabled? %>
    <div class="job-description">
        <div class="row">
           
                <!-- Left Inner -->
                <div class="col-md-9">
                    <div class="left-inner">
                        <div class="row">
                            <h1 ><%= @project.name %></h1>
                            <br />
                        </div>
                        <div class="row">
                        <% if @project.pictures.present? %>
                        <%= image_tag @project.pictures.first.url, size: "600", :style=>"max-height: 400px" %>
                        <% else %>
                        <img src="<%= asset_path( 'thumbnail-default.jpg' ) %>" alt="">
                        <% end %>
                        </div>
                        <div class="row">
                            <p><%= @project.annotation %></p>
                        </div>
                        <div class="row gallery">
                        <h4>Detailne o projekte</h4>
                        <p><%= raw @project.about %></p>
                        </div>

                       
                        </div>
                                                
                    <div class="row gallery">
                    <h4>Galéria ku projektu</h4>
                        <% @project.pictures.each_with_index do |pic, index| %>
                        <%= link_to image_tag(pic.url(:resized).to_s), pic.url %>
                        <% end %>
                        </div>
               
            </div>
                <!-- End Left Inner -->

                <!-- Right Inner -->
                <div class="col-md-3">
                    <div class="right-inner">
                        <div class="row">
                            <h1 style="color:white">Detaily</h1>
                            <br />
                        </div>
                    	<div class="row">
                             <p class="hex">Cieľová suma: <%= @project.amount %></p>
                        <p class="hex">Ostáva: <%= TotalToEnd(@project.id) %></p>
                        <p class="hex">Kategória: <%= @project.category.name %></p>
                        <hr>

                        <div class="progression">
                            <h5>Vyzbieraných <span class="pull-right"><%= projectCollectedMoneyPerc(@project.id) %>%</span></h5>
                            <div class="progress progress-u progress-xxs">
                                    <div class="progress-bar progress-bar-u-yellow" role="progressbar" aria-valuenow="<%= projectCollectedMoneyPerc(@project.id) %>" aria-valuemin="0" aria-valuemax="100" style="width: <%= projectCollectedMoneyPerc(@project.id) %>%">
                                    </div>
                                </div>
                        </div>
                    </div>
                        <h4>Autor projektu</h4>
                        <% if @project.user.picture.url(:resized).present? %>
                                <%= image_tag @project.user.picture.url(:resized).to_s %>
                            <% else %>
                                <img class="img-responsive profile-img margin-bottom-20" src="<%= asset_path( 'user-avatar.png' ) %>" alt="">
                        <% end %>
                        <div class="overflow-h">
                            <span class="font-s"><%= @project.user.name %></span>

                            <p class="color-green"><span class="hex"><%=raw %(#{link_to(@project.user.personalweb, url_with_protocol(@project.user.personalweb), :target => '_blank')}) %></span></p>
                            <ul class="social-icons">
                                <li><a class="social_facebook" data-original-title="Facebook" href="#"></a></li>
                                <li><a class="social_googleplus" data-original-title="Google Plus" href="#"></a></li>
                                <li><a class="social_twitter" data-original-title="Twitter" href="#"></a></li>
                            </ul>
                        </div>

                        <hr>

                        <ul class="list-unstyled save-job">
                            <li><i class="fa fa-eye"></i> <a href="#">Pozri autorove dalsie projekty</a></li>
                            <li><i class="fa fa-plus"></i> <a href="#">Sleduj projekt</a></li>
                        </ul>

                        <hr>
                        <%= link_to " Podpor projekt", new_payment_path(:project_id=>@project.id), :class=>"btn-u btn-block fa fa-eur" %>
                         
                        <h4>Odmeny</h4>
                        <% if @project.rewards.any? %>
                        <% @project.rewards.each do |p| %>
                        <div class="people-say margin-bottom-20">
                            <div class="overflow-h">
                                <span><%= p.name %></span>
                                <span class="hex pull-right" style="padding-right:3px">hodnota: <%= p.amount %> <i class="fa fa-eur"></i></span>
                                <p><%= p.about %></p>
                            </div>
                        </div>
                        <% end %>
                        
                        <% else %>
                        <p> Projekt nemá žiadne odmeny </p>
                        <% end %>
                        <hr>
                        <h4>Darcovia</h4>
                        <% if @project.payments.any? %>
                        <% @project.payments.each do |pay| %>
                        <div class="people-say margin-bottom-20">
                                <% if pay.user.name? %>
                                <span><%= pay.user.name %></span>
                                <% else %>
                                <span>Anonym</span>
                                <% end %>
                                <span class="hex pull-right" style="padding-right:3px">prispel: <%= pay.amount %> <i class="fa fa-eur"></i></span>
                            
                        </div>
                        <% end %>
                        <% end %>

                    </div>
                </div>
                <!-- End Right Inner -->
            </div>
        </div>
    </div>

<% else %>
<% controller.redirect_to root_path %>
<% end %>
